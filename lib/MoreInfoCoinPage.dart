import 'package:flutter/material.dart';

class MoreInfoCoinPage extends StatelessWidget {
  const MoreInfoCoinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
